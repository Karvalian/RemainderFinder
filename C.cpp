#include <bits/stdc++.h>

using namespace std;

int main(){
	ios::sync_with_stdio(false);
	cin.tie(0);
	// Amount of times the program has to run
	int t;
	cin >> t;
	// end
	while(t--){
		// Two numbers to find the remainder of
		int a, b;
		cin >> a >> b;
		// end
		// Simple formula to get the remainder
		cout << a % b << endl;
	}
	cout << "Press any key to exit." << endl;
	int kk;
	cin >> kk;
}
	
}
